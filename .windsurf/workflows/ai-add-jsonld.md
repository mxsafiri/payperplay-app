---
description: Add JSON-LD structured data to a page
---

1. Build a minimal JSON-LD object for the page (WebSite, Organization, Article, etc.).
2. Render it using `JsonLd` from `src/components/seo/jsonld.tsx`.
3. Ensure URLs use `appConfig.site.url` as the base.
4. Do not include sensitive data.
